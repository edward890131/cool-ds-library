# extensions/ — 這個專案新增、框架沒有的東西

當這個專案需要框架沒有的元件 / variant / section，寫在這裡（不要去改框架）。

每個擴充用框架的 schema 格式寫，並在 notes 標 `upstream-候選`（讓 goonsdesign 之後決定要不要收進框架）。

→ 這是「今天的客製 = 明天的模板」的入口。goonsdesign 只看各專案這個資料夾來決定要 harvest 什麼。
